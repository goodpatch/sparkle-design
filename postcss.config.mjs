// PostCSS の設定ファイル
// en: PostCSS configuration file
/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    '@tailwindcss/postcss': {
    },
  },
};

export default config;
